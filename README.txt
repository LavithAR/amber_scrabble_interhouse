AMBER SCRABBLE INTERHOUSE v3 - Render-ready (No timer, manual turns)\nQuick local test:\n1. Install Node.js LTS (if not present).\n2. Extract this folder.\n3. Open a terminal in the folder and run:\n   npm install\n   cd client\n   npm install\n   npm run build\n   cd ..\n   node server.js\4. Open http://localhost:3000/admin (admin) and http://localhost:3000/join (players).\n\nDeploy to Render: push to GitHub, create Web Service, build command: npm install && cd client && npm install && npm run build && cd ..; start command: npm start