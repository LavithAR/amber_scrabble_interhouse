{
  "name": "amber-scrabble-interhouse-v3",
  "version": "1.0.0",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "cd client && npm install && npm run build || true"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "socket.io": "^4.7.2"
    "scripts": {
  "start": "node server.js",
  "build": "echo 'Skipping build step (no frontend build needed)'"
}

  }
}
